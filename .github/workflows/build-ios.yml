name: Build Swiftgram IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Select required Xcode
        run: |
          XCODE_VERSION=$(python3 -c 'import json; print(json.load(open("versions.json"))["xcode"])')
          echo "Using Xcode $XCODE_VERSION"
          sudo xcode-select -s "/Applications/Xcode_${XCODE_VERSION}.app/Contents/Developer"
          xcodebuild -version

      - name: Create canonical source directory
        run: |
          sudo rm -rf /Users/Shared/telegram-ios
          sudo mkdir -p /Users/Shared
          sudo cp -R "$GITHUB_WORKSPACE" /Users/Shared/telegram-ios
          sudo chown -R "$(whoami)" /Users/Shared/telegram-ios

      - name: Build IPA
        run: |
          set -e

          SOURCE_DIR=/Users/Shared/telegram-ios
          BAZEL_USER_ROOT="/private/var/tmp/_bazel_containerhost"

          cd "$SOURCE_DIR"

          BUILD_NUMBER_OFFSET="$(cat build_number_offset)"
          COMMIT_COUNT="$(git rev-list --count HEAD)"
          BUILD_NUMBER="$((COMMIT_COUNT + BUILD_NUMBER_OFFSET))"

          echo "Build number: $BUILD_NUMBER"

          python3 build-system/Make/ImportCertificates.py \
            --path build-system/fake-codesigning/certs

          python3 -u build-system/Make/Make.py \
            --bazelUserRoot="$BAZEL_USER_ROOT" \
            build \
            --configurationPath="build-system/appstore-configuration.json" \
            --codesigningInformationPath=build-system/fake-codesigning \
            --configuration=release_arm64 \
            --buildNumber="$BUILD_NUMBER"

          mkdir -p "$GITHUB_WORKSPACE/output"

          IPA_PATH=$(find bazel-out -type f -name "Telegram.ipa" | head -n 1)

          if [ -z "$IPA_PATH" ]; then
            echo "Telegram.ipa not found"
            exit 1
          fi

          echo "Found IPA: $IPA_PATH"
          cp "$IPA_PATH" "$GITHUB_WORKSPACE/output/Swiftgram.ipa"

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: Swiftgram-IPA
          path: output/Swiftgram.ipa
          if-no-files-found: error
